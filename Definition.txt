DOM : document object model

当网页加载时，浏览器会创建该页面的DOM. 

HTML DOM是一组 tree of objects, 有 Document, <html>, <head>, <body>等组成, 在DOM中，所有HTML element 都是object

-- 换句话说：HTML DOM是如何获取，更改，添加或删除HTML元素的标准。

HTML DOM is the standard for manipulating HTML elements (get change add delete)